<% chocolatier = Chocolatier.find_by("id" => params["id"]) %>

<h1>Edit Chocolatier #<%= chocolatier["id"] %></h1>

<!-- This won't work!  It will send a GET request instead. -->
<form action="/chocolatier/<%= chocolatier["id"] %>" method="patch">

  <p>
    <label>What is the name of the Chocolatier?</label>
    <input type="text" name="full_name" value="<%= chocolatier["name"] %>">
  </p>

  <p>
    <label>Do you have a link to a photo?</label>
    <input type="text" name="url" value="<%= chocolatier["photo_url"] %>">
  </p>

  <input type="submit"> or <a href="/chocolatier">Cancel</a>

</form>